{
  "name": "fg-label",
  "private": true,
  "scripts": {
    "start": "docker-compose -f infra/docker-compose.yml up -d --build",
    "stop": "docker-compose -f infra/docker-compose.yml down"
  },
  "dependencies": {
    "@ant-design/icons": "^6.0.0",
    "antd": "^5.24.8"
  }
}
